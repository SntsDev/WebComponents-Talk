export const styles = `ul {
  display: flex;
  list-style-type: none;
  margin: 0;
  padding: 0.5em;
  box-shadow: 0 0 10px 2px rgba(50,50,50,0.2);
}

li { margin-right: 1em; }

a { 
  color: var(--sl-color-primary-700, blue); 
}

`;
